services:
  app:
    build:
      context: ..
      dockerfile: local/Dockerfile
      args:
        APP_RELEASE_VERSION: ${APP_RELEASE_VERSION:-}
        APP_BUILD_SHA: ${APP_BUILD_SHA:-}
        APP_VERSION: ${APP_VERSION:-}
        APP_VERSION_TOKEN: ${APP_VERSION_TOKEN:-}
    restart: unless-stopped
    ports:
      - "${SUBBOOST_PORT:-3000}:3000"
    volumes:
      - ${SUBBOOST_DATA_DIR:-./data}:/data
    user: "1000:1000"
    environment:
      DATABASE_URL: ${DATABASE_URL:-file:/data/subboost.db}
      DATABASE_PATH: ${DATABASE_PATH:-/data/subboost.db}
      LEGACY_DATABASE_URL: ${LEGACY_DATABASE_URL:-}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:?set ENCRYPTION_KEY}
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET}
      APP_URL: ${APP_URL:-http://localhost:3000}
      APP_RELEASE_VERSION: ${APP_RELEASE_VERSION:-}
      APP_BUILD_SHA: ${APP_BUILD_SHA:-}
      APP_VERSION: ${APP_VERSION:-}
      APP_VERSION_TOKEN: ${APP_VERSION_TOKEN:-}
